package com.lordsai.lsi.entity.enums;

/** IN_PROGRESS until the student submits; EXPIRED when the deadline passed without a submission. */
public enum ExamAttemptStatus {
    IN_PROGRESS, SUBMITTED, EXPIRED
}
